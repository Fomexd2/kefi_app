name: kefi_app
description: Control inteligente de tus lotes de kefir
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  hive_flutter: ^1.1.0
  flutter_riverpod: ^2.5.1
  flutter_local_notifications: ^17.2.3
  timezone: ^0.9.4
  go_router: ^14.2.7
  intl: ^0.20.2
  uuid: ^4.4.2
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
